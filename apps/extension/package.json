{
  "name": "@gj/extension",
  "version": "0.1.1",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "build:test": "GJ_TEST=1 wxt build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test:unit": "vitest run",
    "postinstall": "wxt prepare"
  },
  "dependencies": {
    "@gj/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1",
    "wxt": "^0.21.4"
  }
}
